# TopoEval — 产品设计文档

> AI 3D 拓扑低模评测工具 · 产品设计层面完整说明
> 版本：Phase 1（当前版本）· 日期：2026-07-26

---

## 目录

1. [产品概览](#1-产品概览)
2. [用户场景与工作流](#2-用户场景与工作流)
3. [页面架构与路由](#3-页面架构与路由)
4. [功能模块详解](#4-功能模块详解)
5. [评测体系设计](#5-评测体系设计)
6. [交互设计细节](#6-交互设计细节)
7. [3D 可视化系统](#7-3d-可视化系统)
8. [状态管理与数据流](#8-状态管理与数据流)
9. [数据持久化策略](#9-数据持久化策略)
10. [视觉设计系统](#10-视觉设计系统)
11. [技术架构概览](#11-技术架构概览)
12. [当前已知限制](#12-当前已知限制)

---

## 1. 产品概览

### 1.1 产品定位

TopoEval 是一款面向游戏美术和 TA（技术美术）的 3D 低模拓扑质量评测工具。它通过**自动算法检测 + 人工逐条审核**相结合的方式，对低多边形（low-poly）模型的拓扑结构进行系统性打分，帮助美术人员快速定位拓扑问题并量化模型质量。

### 1.2 核心价值

| 痛点 | 解决方案 |
|------|----------|
| 拓扑质量靠肉眼检查，效率低 | 自动检测 6 项拓扑指标（三角面占比、N-gon、非流形边、重叠面、极点、密度），3D 视图实时高亮 |
| 评价标准不统一 | 内置静态/可动两套评测标准，共 9 个评测维度，明确评分阈值 |
| 缺乏量化打分机制 | 逐条 1-10 打分 + 自动映射到维度满分，最终汇总为百分制总分 |
| 多模型对比困难 | 高低模双视口同步浏览，相机联动 |

### 1.3 目标用户

- **游戏美术人员**：评估自己制作的模型的拓扑质量
- **技术美术（TA）**：制定/审核模型规范，批量检查外包模型
- **面试/评测场景**：作为标准化工具测试候选人的模型制作能力

---

## 2. 用户场景与工作流

### 2.1 主工作流：单模型评测

```
首页 → 上传模型(.obj/.fbx) → 自动分析 → 开始逐条审核 → 查看结果 → 保存记录
```

**详细步骤：**

1. **上传模型**：用户在首页点击"加载模型开始评测"，或通过导航进入模型查看器页面，拖放/选择 .obj 或 .fbx 文件
2. **自动分析**：系统自动执行 6 项检测（面型分类、非流形边、重叠面、极点、密度分布、循环线），在 3D 视图中渲染高亮叠加层
3. **选择模型类型**：用户在右侧面板切换"静态模型"或"可动模型"（影响评测标准的选择和评分权重）
4. **逐条审核**：点击"开始逐条审核"，右侧面板展示评测列表，用户逐条展开评分（1-10 分制）
   - 每条自动提供检测数据和推荐打分参考
   - 3D 视图自动高亮当前评测项对应的几何特征
   - 可前后导航，也可直接点击列表项跳转
5. **完成审核**：所有条目打分后点击"完成"，显示总分、雷达图、维度得分明细
6. **保存记录**：点击"保存评测结果"，写入评测历史

### 2.2 辅助工作流：高低模对比

```
首页 → 上传低模 + 上传高模(参考) → 双视口同步浏览 → 逐条审核低模 → 保存
```

**特点：**
- 左侧视口显示高模（参考），右侧显示低模（评测对象）
- 两侧相机同步旋转/缩放，方便比对结构差异
- 低模视口支持所有高亮叠加，高模视口为实体渲染

### 2.3 辅助工作流：浏览评测标准

```
导航栏 → 评估标准页面 → Tab 切换静态/可动模型标准 → 查看各维度评分细则
```

### 2.4 辅助工作流：查看历史记录

```
导航栏 → 评测历史 → 展开记录查看雷达图/维度得分 → 可删除
```

---

## 3. 页面架构与路由

### 3.1 页面清单

| 路由 | 页面组件 | 功能说明 |
|------|----------|----------|
| `/` | DashboardPage（首页） | 产品入口，最近模型列表 + 最近评测记录 + 功能预览 |
| `/viewer` | UploadPage | 模型上传页（模型查看器的入口状态） |
| `/viewer/single` | ViewerPage | 单模型查看 + 评测（三栏布局：3D视口 + 工具栏 + 评测面板） |
| `/viewer/compare` | ComparePage | 高低模对比 + 评测（双视口 + 工具栏 + 对比评测面板） |
| `/standards` | StandardsPage | 评测标准展示（静态/可动两套标准 Tab 切换） |
| `/history` | HistoryPage | 评测历史记录列表（展开查看详情/雷达图） |

### 3.2 布局结构

所有页面共享同一外层布局（AppLayout）：

```
┌──────────────────────────────────────────────┐
│  Header (48px): Logo + 导航栏                 │
├──────────────────────────────────────────────┤
│                                               │
│  <Outlet />  ← 页面内容区（flex-1, 撑满剩余高度）│
│                                               │
└──────────────────────────────────────────────┘
```

单模型评测页（ViewerPage）三栏布局：

```
┌───────────────────────┬────┬──────────────┐
│                       │工  │              │
│   3D 视口 (flex-1)    │具  │ 评测面板     │
│                       │栏  │ (27rem宽)   │
│                       │    │ 可滚动      │
└───────────────────────┴────┴──────────────┘
```

对比评测页（ComparePage）三栏布局：

```
┌─────────────────┬─┬─────────────────┬────┬──────────────┐
│ 高模视口(flex-1)│ │ 低模视口(flex-1)│工  │ 对比评测面板 │
│   实体渲染      │ │ 实体+高亮叠加   │具  │ (27rem宽)   │
│                 │ │                 │栏  │             │
└─────────────────┴─┴─────────────────┴────┴──────────────┘
```

---

## 4. 功能模块详解

### 4.1 模型上传模块

**涉及组件：** `ModelDropZone`

**支持格式：**
- OBJ（推荐，支持完整面数据解析）
- FBX（实验性，仅支持三角面统计，不支持原始面型检测）

**限制：**
- 单模型最大 100MB
- 高模参考文件最大 1024MB（仅对比页）

**交互状态：**
- 默认态：虚线边框拖放区 + 选择文件按钮
- 拖拽悬停态：边框加深 + 轻微缩放（scale 1.01）
- 加载态：旋转动画 + "正在解析模型..."
- 错误态：警告图标 + 错误信息 + 重新选择链接

**拖放支持：** 自定义 `useDragAndDrop` hook，监听 dragenter/dragover/dragleave/drop 事件

---

### 4.2 3D 模型浏览模块

**涉及组件：** `ViewerCanvas`, `ViewerScene`, `LoadedModel`, `ModelInfoOverlay`, `CompareCanvas`, `CompareScene`, `DualViewport`, `SyncedOrbitControls`

**渲染引擎：** Three.js 0.185 + @react-three/fiber（React 声明式 3D）

**三种渲染模式：**

| 模式 | 视觉效果 | 适用场景 |
|------|----------|----------|
| 线框（wireframe） | 仅显示三角面边线 | 观察布线走向 |
| 线框+实体（wireframe-solid） | 灰色面 + 深色线框叠加 | 同时观察面和线 |
| 白模（solid） | 纯色实体渲染，无纹理 | 观察形体轮廓、密度高亮 |

**视角控制：**
- 鼠标拖拽旋转
- 滚轮缩放
- 右键平移
- 双视口模式（高低模对比）下相机同步联动

**模型归一化：** 所有模型加载后自动居中缩放，包围盒最大维度归一化到合适尺寸

**模型信息浮层：** 视口左上角显示：
- 顶点数、面数
- 面型分布（四边面/三角面/N-gon数量和占比）
- 部件数、包围盒尺寸
- 材质数、格式

---

### 4.3 工具栏模块

**涉及组件：** `ViewerToolbar`, `MaterialControls`

**功能按钮（纵向排列）：**

1. **渲染模式切换**：线框 / 线框+实体 / 白模（三选一）
2. **网格显示**：Toggle 地面参考网格
3. **材质设置**：弹出浮层，调节材质颜色、粗糙度、金属度
4. **重置视角**：相机回到默认位置
5. **清除模型**：移除当前模型，恢复上传页状态

---

### 4.4 自动检测模块

**涉及文件：** `topology-analyzer.ts`（~730 行）

**6 项自动检测：**

| 检测项 | 算法要点 | 输出 |
|--------|----------|------|
| 面型统计 | OBJ 原始面数据遍历，不依赖三角化后的 geometry | 四边面/三角面/N-gon 数量、占比 |
| 非流形边 | 位置去重（精度 0.0001），统计每条边被多少面共享，≥3 面共用即为非流形 | 非流形边数量 + 每条边的 3D 坐标 |
| 重叠面 | 基于 OBJ 原始面片（非三角化后），比较重心重合 + 3 个以上顶点重合 | 重叠面对数量 + 原始面索引对 |
| 极点 | 基于 OBJ 原始面构建邻接图（非三角化），计算每个顶点的价（valence），六极点及以上（≥6 条边共顶点）标记 | 极点数量 + 世界坐标 |
| 密度分布 | 遍历三角面，计算每个顶点周围面的平均面积，取倒数作为局部密度，min-max 归一化到 [0,1] | 每顶点密度值 + 位置→密度映射 |
| 循环线 | 仅适用于四边面为主的网格。构建边对面映射 → 计算四边面中对边关系 → 追踪闭合回路 | 循环线数量 + 每条线的边坐标 |

**关键设计决策：**
- 极点/面型检测优先使用 OBJ 原始面数据（非三角化后），因为这反映美术人员的实际拓扑，三角化会人为增加价数
- 密度检测使用三角化后的 geometry（因为需要准确的面积计算）

---

### 4.5 评测审核模块

**涉及组件：** `EvalPanel`, `CompareEvalPanel`, `FlowReviewCard`

**这是整个产品最核心的交互模块。** 流程分为三个阶段：

#### 阶段一：审核前（初始状态）

- 右侧面板显示：文件信息区 + 模型类型切换（静态/可动）+ 参考模型上传（可选）
- 自动检测报告已完成（模型加载后自动触发）
- 显示"开始逐条审核"按钮（蓝色强调）

#### 阶段二：逐条审核中

**评测列表：** 所有评测维度下所有条目平铺展示

每条显示：
- 条目名称 + 当前评分（x/10）
- 进度条（彩色，按维度着色）
- 维度名称 + 满分分数

**交互规则：**
- 点击某条 → 展开审核卡片，3D 视图高亮对应几何特征
- 再次点击同一条 → 收起
- 点击另一条 → 展开新条（旧条自动收起），同时跳转到该条目
- 展开/收起使用 CSS Grid 动画（`grid-rows-[1fr]` ↔ `grid-rows-[0fr]` + opacity 过渡）
- 当前条左侧有蓝色竖线标记

**审核卡片内容（FlowReviewCard）：**
1. 条目详细描述（来自评测标准定义）
2. 自动检测数据（如"三角面 120 个，占比 15.3%，总面数 782"）
3. 推荐打分（灯泡图标 + AI 建议分数区间 + 评分规则）
4. 1-10 打分圆点（点击设置分数，已选中的变蓝并放大）
5. 当前分数 + 已评进度（已评数/总数）
6. 导航按钮：上条 / 下条 / 完成（最后一条时显示"完成"按钮）

**3D 高亮联动：**

| 评测项 | 高亮颜色 | 高亮类型 |
|--------|----------|----------|
| 四边/三角面比例 | 橙色 `#ff9500` | 三角面填充 |
| 三角面分布合理性 | 橙色 `#ff9500` | 三角面填充 |
| N-gon 数量 | 红色 `#ff3b30` | N-gon 面填充 |
| 极点分布 | 蓝色 `#4a90d9` | 蓝色圆点标记 |
| 非流形边 | 红色 `#ff3b30` | 红色线段 |
| 重叠面 | 紫色 `#af52de` | 重叠面填充 |
| 密度分布合理性 | 蓝-红渐变 | 全模顶点着色 |
| 循环线完整性 | 绿色 `#34c759` | 绿色线段 |

**渲染模式切换：**
- 评测"密度分布合理性"时，自动切换到白模模式（密度颜色在白模上才清晰可见）
- 切换到其他条目时，恢复之前的渲染模式

**高亮技术实现：**
- 合并所有子网格的 geometry 到一个 BufferGeometry
- 构建 OBJ 原始面索引 → 三角化面索引的映射
- 根据评测项选择目标原始面，提取对应三角形索引
- 归一化变换（scale + translate）与 LoadedModel 保持一致，确保高亮层与模型精确对齐

#### 阶段三：审核完成

显示完整结果：
- **总分**：大字显示（32px），红/黄/绿三色分级（<40%红色，40-70%黄色，>70%绿色）
- **已评条数统计**
- **重新开始逐条审核**按钮
- **雷达图**：多维得分分布可视化（4 层同心网格 + 标签）
- **维度得分列表**：每条维度的得分/满分 + 百分比
- **逐条评分详情**：按维度分组，每项显示原始分 → 映射分
- **保存评测结果**按钮：写入 localStorage，跳转后不可撤销

---

### 4.6 评测标准展示模块

**涉及组件：** `StandardsPage`, `StandardCard`, `DimensionSection`, `CriterionRow`

**页面结构：**
- 页面标题 + 描述
- Tab 切换：静态模型 / 可动模型
- 标准卡片：总分 + 自动/人工统计
- 维度列表（分隔线分隔，每项可展开）：
  - 维度名称 + 权重分
  - 维度下所有评测条目：名称 + 满分 + 检测方式（自动/人工） + 评分规则 + 详细描述

---

### 4.7 评测历史模块

**涉及组件：** `HistoryPage`, `HistoryCard`

**列表视图：**
- 每条记录显示：模型名 + 格式 + 大小 + 日期 + 总分（彩色）+ 评级（优/中/差）

**展开详情：**
- 雷达图（200px）
- 维度得分列表
- 自动检测得分 / 人工评测得分
- 删除按钮

**空状态：** 暂无评测记录提示 + 跳转到模型查看器按钮

---

## 5. 评测体系设计

### 5.1 两套评测标准

| | 静态模型标准 | 可动模型标准 |
|------|------|------|
| 总分 | 100 | 100 |
| 维度数 | 3 | 4 |
| 条目数 | 9 | 9 |
| 自动检测 | 4 项 | 4 项 |
| 人工评测 | 5 项 | 5 项 |

### 5.2 静态模型评测维度

**维度一：面型质量（权重 30）**

| 条目 | 满分 | 检测方式 | 规则 |
|------|------|----------|------|
| 四边/三角面比例 | 10 | 自动 | <15K面 ≤30%, 15-25K ≤20%, >25K ≤10%，超出每 1% 扣 1 分 |
| 三角面分布合理性 | 8 | 人工 | 三角面应避开关节/动画关键区，1-10 打分 |
| 极点分布 | 6 | 人工 | ≥6 边共顶点为极点，需避开关节/高光区，1-10 打分 |
| N-gon 数量 | 6 | 自动 | 每个 N-gon 扣 1 分，扣完为止 |

**维度二：面错误（权重 25）**

| 条目 | 满分 | 检测方式 | 规则 |
|------|------|----------|------|
| 非流形边 | 15 | 自动 | 每条扣 0.5 分，扣完为止 |
| 重叠面 | 10 | 自动 | 每组扣 0.5 分，扣完为止 |

**维度三：布线合理性（权重 45）**

| 条目 | 满分 | 检测方式 | 规则 |
|------|------|----------|------|
| 结构跟随性 | 18 | 人工 | 边线沿结构轮廓分布，偏差大则≤6分，1-10 打分 |
| 密度分布合理性 | 14 | 人工 | 关键区高密度，平坦区低密度，1-10 打分 |
| 循环线完整性 | 13 | 人工 | 环形边回路完整连续，1-10 打分 |

### 5.3 可动模型评测维度

**维度一：面型质量（权重 15）**

| 条目 | 满分 | 检测方式 |
|------|------|----------|
| 四边/三角面比例 | 5 | 自动 |
| 三角面分布合理性 | 4 | 人工 |
| 极点分布 | 3 | 人工 |
| N-gon 数量 | 3 | 自动 |

**维度二：面错误（权重 20）**

| 条目 | 满分 | 检测方式 |
|------|------|----------|
| 非流形边 | 12 | 自动 |
| 重叠面 | 8 | 自动 |

**维度三：布线合理性（权重 25）**

| 条目 | 满分 | 检测方式 |
|------|------|----------|
| 结构跟随性 | 10 | 人工 |
| 密度分布合理性 | 8 | 人工 |
| 循环线完整性 | 7 | 人工 |

**维度四：绑定动画友好性（权重 40）** 👈 可动模型特有

| 条目 | 满分 | 检测方式 |
|------|------|----------|
| 可动部位面数支撑 | 20 | 人工 |
| 可动部位环形线 | 20 | 人工 |

### 5.4 评分映射算法

**逐条审核模式：**
```
条目实际得分 = Math.round((用户打分/10) × 条目满分)
总得分 = Σ 所有条目实际得分
```

**传统模式（自动+人工混合）：**
```
自动检测得分 = 根据各条目的扣分规则直接计算
人工评测得分 = 五档评级映射（糟糕0%, 差25%, 普通50%, 良好75%, 优秀100%）× 条目满分
总得分 = 自动检测得分 + 人工评测得分
```

---

## 6. 交互设计细节

### 6.1 动画效果

| 场景 | 动画 | 实现方式 |
|------|------|----------|
| 评测条目展开/收起 | 高度渐变 + 透明度过渡 | CSS `grid-rows-[0fr]` ↔ `grid-rows-[1fr]` + `transition-[grid-template-rows,opacity] duration-300` |
| 打分进度条填充 | 宽度渐变 | CSS `transition-all duration-300` |
| 打分按钮 | 点击缩放 | CSS `scale-105` on active |
| 拖放区域悬停 | 轻微放大 | CSS `scale-[1.01]` |
| 按钮 Hover | 背景色过渡 | CSS `transition-colors duration-150/200` |
| 材质面板弹出 | 绝对定位浮层 | `absolute right-full mr-3` |

### 6.2 颜色语义

| 颜色 | 语义 | 使用场景 |
|------|------|----------|
| 蓝色 `#0071e3`（accent） | 主要操作 | 按钮、当前选中、打分激活 |
| 绿色 `#34c759` | 通过/优秀 | 高分标记、循环线高亮 |
| 琥珀色 `#ff9500` | 警告/中等 | 中分标记、三角面高亮 |
| 红色 `#ff3b30` | 错误/低分 | 低分标记、N-gon/非流形边高亮 |
| 紫色 `#af52de` | 信息 | 重叠面高亮 |

### 6.3 评分色彩分级

| 得分率 | 颜色 | 评级 |
|--------|------|------|
| < 40% | 红色 `text-red-500` | 差 |
| 40%-70% | 琥珀色 `text-amber-500` | 中 |
| > 70% | 绿色 `text-emerald-500` | 优 |

### 6.4 导航

**顶部导航栏（Header）：**
- Logo（TE 图标 + TopoEval）
- 导航链接：首页、模型查看器、评估标准、评测历史
- 当前页高亮（深色背景 + 粗体）

**面包屑/回退：**
- 对比页无模型时自动跳转回单模型页
- 清除模型后跳转到首页

### 6.5 状态反馈

| 状态 | 设计 |
|------|------|
| 加载中 | 旋转动画 + 文字提示 |
| 错误 | 警告图标 + 错误信息 + 操作入口 |
| 空数据 | 图标 + 说明文字 + CTA 按钮 |
| 成功 | 无特殊提示（静默完成） |
| 已保存 | 按钮变灰 + 文字变为"已保存" |

---

## 7. 3D 可视化系统

### 7.1 场景组织

**单模型场景（ViewerScene）：**
```
Canvas
├── ambientLight（环境光）
├── directionalLight（主方向光 + 阴影）
├── Grid（参考网格，可隐藏）
├── LoadedModel（模型主体）
│   ├── 归一化 Scale 层
│   └── 居中 Translation 层
│       └── Mesh（材质：PBR MeshStandardMaterial）
├── HighlightOverlay（高亮叠加层）👈 关键
│   ├── 归一化 Transform（与模型一致）
│   ├── Face Highlight（面片高亮 Mesh）
│   │   ├── 三角面 / N-gon / 重叠面：单色半透明
│   │   └── 密度分布：逐顶点颜色映射
│   ├── Point Highlight（极点圆点 Points）
│   └── Line Highlight（非流形边/循环线 LineSegments）
└── OrbitControls（相机控制）
```

**双模型场景（CompareScene）：**
- 两个独立 Viewport，各自有 OrbitControls 但同步
- 左侧高模：实体渲染，无高亮
- 右侧低模：实体渲染 + 高亮叠加

### 7.2 高亮叠加技术

**核心挑战：** 高亮几何体必须与模型渲染的三角化后几何体精确对齐。

**解决方案：**
1. 遍历所有子 Mesh，合并 position 和 index 到一个 BufferGeometry
2. 维护 OBJ 原始面索引 → 三角化面索引的映射表
3. 选择目标原始面 → 提取对应三角形索引 → 构建高亮 geometry
4. 应用与 LoadedModel 相同的归一化变换（scale + translate）
5. 使用 polygonOffset（-10, -10）避免 z-fighting
6. 极点/线条略微放大（scale 1.002）避免与模型表面重叠

### 7.3 材质系统

**默认材质：** PBR MeshStandardMaterial
- 颜色：`#d0d0d0`（中性灰）
- 粗糙度：0.4
- 金属度：0
- 支持实时调节（MaterialControls 面板）

**高亮材质：**
- 面片：MeshBasicMaterial（不受光照影响），半透明（opacity 0.5），DoubleSide
- 密度：vertexColors 模式，红→蓝渐变，opacity 0.65
- 线条：LineBasicMaterial，opacity 0.9
- 极点：PointsMaterial + 圆形纹理（Canvas 生成），size 0.048

---

## 8. 状态管理与数据流

### 8.1 Store 职责划分

项目使用 Zustand 5 进行状态管理，共 6 个独立 Store：

| Store | 职责 | 持久化 |
|-------|------|--------|
| `modelStore` | 单模型加载/卸载（currentModel, modelObject, stats, objFaceData, referenceModel） | session（仅内存） |
| `compareStore` | 对比双模加载/卸载（highModel, lowModel 两个 ModelSlot） | session |
| `evalStore` | 评测核心状态（modelType, autoReport, manualRatings, flowReviewScores, flowTotal, flowSaved） | session |
| `evalFlowStore` | 逐条审核流程状态（isActive, currentIndex, criteria[], reviewScores） | session |
| `highlightStore` | 当前高亮项（criterionId） | session |
| `viewerStore` | 3D 视图设置（renderMode, showGrid, autoRotate, materialColor/roughness/metalness） | localStorage |
| `evalHistoryStore` | 评测历史记录（records[]） | localStorage |

### 8.2 核心数据流

```
用户上传模型
    ↓
modelStore.loadModel() / compareStore.loadLowPolyModel()
    ↓
模型解析 (model-parser.ts)
    ↓
拓扑分析 (topology-analyzer.ts)  ← 自动检测
    ↓
evalStore.setAutoReport()
    ↓
用户点击"开始逐条审核"
    ↓
evalFlowStore.startFlow()
    ↓
用户逐条打分 → evalFlowStore.setScore()
    ↓           ↓
3D高亮联动   评测列表展开/收起
(highlightStore.setCriterion)
    ↓
用户点击"完成"
    ↓
evalStore.setFlowResult() → 显示总分、雷达图、维度得分
    ↓
用户点击"保存"
    ↓
evalHistoryStore.addRecord() → localStorage 持久化
```

### 8.3 高亮同步机制

```
评测面板点击条目
    ↓
highlightStore.setCriterion(criterionId)
    ↓
HighlightOverlay 组件订阅 criterionId
    ↓
getHighlightData(criterionId, model, faceData, report)
    ↓
根据 criterionId 分支：
  - quad-tri-ratio / tri-distribution → 三角面集合
  - ngon-count → N-gon 面集合
  - overlapping → 重叠面对集合
  - pole-distribution → 极点坐标数组
  - non-manifold → 非流形边坐标数组
  - density → 全模顶点颜色
  - loop-edges → 循环线坐标数组
    ↓
渲染对应高亮 geometry 到 3D 场景
```

---

## 9. 数据持久化策略

### 9.1 持久化内容

| 数据 | 存储方式 | Key | 上限 |
|------|----------|-----|------|
| 3D 视图设置 | localStorage（Zustand persist） | `topoeval-viewer-settings` | 无 |
| 评测历史记录 | localStorage（Zustand persist） | `topoeval-history` | 最近 100 条 |
| 最近模型列表 | localStorage | `topoeval-recent-models` | 最近 20 个 |

### 9.2 不持久化的内容

- 当前加载的 3D 模型对象（Three.js Group，无法序列化）
- OBJ 面数据
- 自动检测报告
- 逐条审核进行中的打分（刷新页面后清零）

### 9.3 评测历史记录数据结构

```typescript
{
  id: string                    // eval_timestamp_random
  modelName: string             // 文件名
  modelFormat: 'obj' | 'fbx'
  modelFileSize: number         // 字节
  modelType: 'static' | 'dynamic'
  createdAt: string             // ISO 8601
  autoTotal: number             // 自动检测得分
  manualTotal: number           // 人工评测得分
  total: number                 // 总分
  maxTotal: number              // 满分
  dimensionScores: [{           // 各维度得分
    dimensionId: string
    dimensionName: string
    score: number
    maxScore: number
  }]
  autoReport: TopologyReport    // 完整自动检测报告（含位置数据）
  manualRatings: Record<string, RatingLevel>
  reviewScores?: Record<string, number>  // 逐条审核原始分
}
```

---

## 10. 视觉设计系统

### 10.1 设计语言

- **风格：** Apple HIG 风格 — 大圆角、半透明玻璃质感（glass morphism）、极简留白
- **配色：** 中性色为主（黑白灰阶梯），蓝色 accent 引导操作，红/黄/绿三元色表达评分等级
- **字体：** 系统默认无衬线字体，等宽数字（mono）用于分数/统计
- **圆角体系：** 小圆角（rounded-lg = 8px）、大圆角（rounded-2xl = 16px）、全圆（rounded-full）

### 10.2 玻璃质感样式

```css
.glass       → bg-white/72 + backdrop-blur-xl + border-black/5
.glass-btn   → bg-black/[0.04] + hover:bg-black/[0.06]，全圆角按钮
.glass-btn-accent → 蓝色背景 + 白色文字，全圆角强调按钮
```

### 10.3 空间系统

- 页面内边距：`px-8 py-10` 或 `py-12`
- 卡片内边距：`p-4` (16px)
- 元素间距：`space-y-1` ~ `space-y-7` + `gap-1` ~ `gap-4`
- 评测面板宽度：固定 `w-[27rem]` (432px)
- Header 高度：固定 `h-12` (48px)
- 页面最大内容宽：`max-w-[960px]`（首页）/ `max-w-[840px]`（标准页）/ `max-w-[720px]`（历史页）

### 10.4 文字层级

| 层级 | 大小 | 字重 | 用途 |
|------|------|------|------|
| H1 | 42px bold | 700 | 首页标题 |
| H2 | 20px semibold | 600 | 区块标题 |
| H3 | 18px semibold | 600 | 卡片标题 |
| Body | 14-15px | 400-500 | 正文 |
| Caption | 12-13px | 400-500 | 辅助信息 |
| Micro | 10-11px | 400-600 | 标签、统计数字 |

---

## 11. 技术架构概览

### 11.1 技术选型理由

| 技术 | 理由 |
|------|------|
| React 19 + TypeScript 5.7 | 类型安全，生态成熟 |
| Vite 7 | 极快的开发构建速度 |
| Tailwind CSS v4 | 原子化 CSS，快速迭代 UI |
| Three.js + @react-three/fiber | 声明式 3D 渲染，与 React 生态无缝集成 |
| Zustand 5 | 轻量状态管理，API 简洁，支持 persist + 选择器 |
| React Router 7 | 标准 SPA 路由方案 |
| Vercel | 免费部署，自动 CI/CD，自定义域名 |

### 11.2 不依赖外部图表库

雷达图使用**纯手写 SVG**，不引入 chart.js/recharts 等第三方图表库。优势：
- 零额外依赖
- SVG 自适应缩放
- 完全控制样式和动画

### 11.3 项目文件规模

```
src/
├── components/
│   ├── evaluation/     (8 个组件) — 评测面板核心
│   ├── layout/         (3 个组件) — Header、侧边栏
│   ├── viewer/         (10 个组件) — 3D 场景、工具栏、高亮
│   ├── standards/      (3 个组件) — 评测标准展示
│   ├── ui/             (8 个组件) — 通用 UI 原语
│   └── shared/         (3 个组件) — 加载/错误/空状态
├── pages/              (6 个页面)
├── stores/             (7 个 Store)
├── lib/                (4 个模块) — 拓扑分析、高亮数据、模型解析、常量
├── data/               (1 个文件) — 两套评测标准定义
├── types/              (2 个文件) — 模型和评测类型
├── hooks/              (1 个 Hook) — 拖放事件
└── routes/             (1 个文件) — 路由配置
```

---

## 12. 当前已知限制

### 12.1 功能限制

1. **FBX 支持不完整**：FBX 格式仅支持基础三角面统计，不支持 OBJ 原始面数据级别的检测（面型分类、N-gon 检测、极点和重叠面检测将使用三角化后的 geometry 作为备选方案，精度较低）
2. **仅支持本地文件**：无云端存储/同步，评测结果仅保存在浏览器 localStorage 中
3. **无批量评测**：每次只能评测一个模型，不支持批量导入和批处理
4. **无用户系统**：无登录/注册，无多用户隔离
5. **无导出功能**：评测结果不能导出为 PDF/Excel 等格式
6. **自动化程度有限**：6 项检测中 4 项可自动打分，5 项依赖人工判断。尤其布线质量（结构跟随性、密度分布合理性、循环线完整性）完全依赖人工
7. **无模型编辑能力**：仅能查看和评测，不能修改模型拓扑
8. **无动画预览**：可动模型的"绑定动画友好性"评测依赖静态判断，无法实际播放骨骼动画
9. **参考模型对比不够深入**：高低模对比目前仅支持视觉同步浏览，没有自动结构差异分析
10. **评测标准不可自定义**：静态/可动两套标准是硬编码的，用户无法修改权重或添加自定义维度

### 12.2 技术限制

1. **大模型性能**：合并所有子 Mesh geometry + 构建三角化映射表对大模型（>100K 面）有性能开销
2. **重叠面检测 O(n²)**：基于重心重合的面片对比，面数多时性能下降明显
3. **循环线检测仅支持四边面**：三角面为主的网格无法检测循环线
4. **localStorage 容量限制**：通常 5-10MB，大量评测记录可能超出限制
5. **无 SSR/SEO**：纯客户端渲染，搜索引擎无法索引

### 12.3 浏览器兼容性

- 需要 WebGL 2.0 支持
- 需要 ES2020+ 支持（动态 import、Optional Chaining 等）
- 不支持 IE 11 及更早版本

---

> 本文档覆盖 TopoEval Phase 1 的全部产品设计细节。后续开发计划将在分析本文档后另行制定。
